{
  "name": "cpw",
  "version": "1.0.0",
  "description": "Connor's Personal Website",
  "main": "index.js",
  "scripts": {
    "dev": "bunx @11ty/eleventy --serve",
    "build": "rm -rf _site && bunx @11ty/eleventy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "gh-pages": "^6.1.1"
  },
  "dependencies": {
    "@ibm/plex-sans": "^1.0.0"
  }
}
